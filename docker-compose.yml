services:
  frigate-monitoring:
    build:
      context: .
    container_name: frigate-monitoring
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.yaml:/config/config.yaml:ro
